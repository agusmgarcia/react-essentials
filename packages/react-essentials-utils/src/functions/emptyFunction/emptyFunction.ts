/**
 * A no-operation (noop) function that performs no action.
 *
 * This function can be used as a placeholder or default value
 * when a function is required but no specific implementation is needed.
 */
export default function emptyFunction(): void {}
