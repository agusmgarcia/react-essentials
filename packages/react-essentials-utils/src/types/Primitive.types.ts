/**
 * Represents the basic primitive types in TypeScript.
 *
 * Includes `boolean`, `null`, `number`, `string`, and `undefined`.
 * Useful for type constraints where only primitive values are allowed.
 */
type Primitive = boolean | null | number | string | undefined;

export default Primitive;
